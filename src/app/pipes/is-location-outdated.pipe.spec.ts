import { IsLocationOutdatedPipe } from './is-location-outdated.pipe';

describe('IsLocationOutdatedPipe', () => {
  it('create an instance', () => {
    const pipe = new IsLocationOutdatedPipe();
    expect(pipe).toBeTruthy();
  });
});
