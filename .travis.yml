dist: bionic
language: node_js
node_js:
  - 12
before_install:
  - npm i -g npm@latest
install:
  - npm ci
script:
  - npm test
before_deploy:
  - npm install -g netlify-cli
  - npm run build
deploy:
  provider: script
  script: netlify deploy --dir ./dist
