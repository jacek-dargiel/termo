dist: bionic
language: node_js
node_js:
  - 12
before_install:
  - npm i -g npm@latest
install:
  - npm ci
script:
  - npm test
before_deploy:
  - npm run build
  - npm install -g @sentry/cli
deploy:
  provider: netlify
  dir: dist/
  prod: true
  edge: true
after_deploy: ./travis/sentry-release.sh
